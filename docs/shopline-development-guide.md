# Shopline 主题开发技巧指南

基于 pSEO Blog 系统开发经验总结

---

## 🎯 核心原则

1. **先小后大**：从最简单的模板开始，确保语法正确后再逐步添加功能
2. **即时验证**：每次推送前在本地验证语法
3. **记录错误**：遇到 CLI 错误时，记录错误信息和解决方案
4. **渐进开发**：不要一次性写完所有代码，分批推送验证

---

## 🔧 Shopline Liquid/Handlebars 语法限制

### ❌ 不支持的语法

#### 1. 带索引的 for 循环

```handlebars
{{!-- ❌ 错误 --}}
{{#for item, index in items}}
  {{index}}
{{/for}}

{{!-- ❌ 错误 --}}
{{#for item in items}}
  {{@index}}
{{/for}}

{{!-- ✅ 正确：不使用索引 --}}
{{#for item in items}}
  <div>{{item}}</div>
{{/for}}
```

**替代方案**：

- 在数据中预先编号
- 使用 CSS 计数器
- 接受不显示序号

#### 2. 复杂的数组操作

```handlebars
{{!-- ❌ 错误：链式过滤器过于复杂 --}}
{{#var result = items | filter("active") | map("name") | join(", ") /}}

{{!-- ✅ 正确：保持简单 --}}
{{#var items_str = items_text | split("||") /}}
```

#### 3. 数组访问语法（点号问题）

```handlebars
{{!-- ❌ 错误：数组索引前有点号 --}}
{{#var parts = text | split("||") /}}
{{parts.[0]}}   {{!-- 不支持 --}}
{{parts.[1]}}   {{!-- 不支持 --}}

{{!-- ✅ 正确：直接使用方括号 --}}
{{#var parts = text | split("||") /}}
{{parts[0]}}    {{!-- 正确 --}}
{{parts[1]}}    {{!-- 正确 --}}
```

**错误信息**：

```
[error]: parse error, line:312 col:27 ,parse expression: qa_parts.[0] | trim()
,error: build expression error,offset:0,error,build variable error,selector start with char:[
```

**修复方法**：

```powershell
# 使用正则表达式批量修复
(Get-Content "file.html" -Raw) -replace '(\w+)\.\[(\d+)\]', '$1[$2]' | Set-Content "file.html" -NoNewline
```

#### 4. 多层嵌套的条件

```handlebars
{{! ⚠️ 尽量避免 }}
{{#if cond1}}
  {{#if cond2}}
    {{#if cond3}}
      深层嵌套可能导致问题
    {{/if}}
  {{/if}}
{{/if}}

{{! ✅ 更好：扁平化逻辑 }}
{{#if cond1}}
  内容1
{{/if}}
{{#if cond2}}
  内容2
{{/if}}
```

---

## 📦 Metafields 设计最佳实践

### 数据格式约定

#### 1. 分隔符规范

- **一级分隔符**：`||` - 用于分隔列表项
- **二级分隔符**：`??` - 用于分隔字段（如标题/描述）
- **避免使用**：`,` 或 `;`（可能出现在正文中）

```
示例：痛点列表
痛点1||痛点2||痛点3

示例：步骤列表（带描述）
步骤标题??步骤描述||第二步标题??第二步描述

示例：FAQ
问题1??答案1||问题2??答案2
```

#### 2. 字段命名规范

- 使用 `custom.` 命名空间
- 字段名全小写，用下划线分隔
- 添加前缀避免冲突（如 `pseo_` 或 `blog_`）

```
✅ 好的命名
custom.pseo_hero_title
custom.blog_reading_time
custom.article_intro

❌ 不好的命名
custom.Title
custom.readingTime
custom.intro
```

#### 3. 字段类型选择

| 内容类型  | 推荐字段类型     | 说明         |
| --------- | ---------------- | ------------ |
| 短标题    | Single line text | 50-200字符   |
| 段落/描述 | Multi-line text  | 无长度限制   |
| 开关      | True/False       | 控制区域显示 |
| 链接      | URL              | 自动验证格式 |
| 日期      | Date             | 自动格式化   |
| 列表数据  | Multi-line text  | 使用分隔符   |

---

## 🚀 Shopline CLI 推送技巧

### 1. 完整推送工作流程

```
┌─────────────────────────────────────────────────────────┐
│  步骤 1：确保代码语法正确                                │
│  - 检查 Liquid 语法                                      │
│  - 移除调试代码                                          │
│  - 保存文件（UTF-8 编码）                                │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 2：打开 PowerShell 并导航到项目目录                │
│  cd e:\Bottle1                                           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 3：执行推送命令                                    │
│  npx @shoplineos/cli theme push \                        │
│    --theme 68c3da6b308b8b294cf01328 \                    │
│    --only "sections/main-article/main-article.html"      │
└─────────────────────────────────────────────────────────┘
                          ↓
                    ┌─────┴─────┐
                    │  成功？    │
                    └─────┬─────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
        是               否               超时
         │                │                │
         ↓                ↓                ↓
   ✅ 刷新页面        🔍 查看错误       🔄 重试
     验证效果          修复后推送         或重新登录
```

### 2. 推送命令格式详解

#### 基本命令结构

```powershell
npx @shoplineos/cli theme push [选项]
```

#### 常用选项

| 选项            | 说明                   | 示例                                               |
| --------------- | ---------------------- | -------------------------------------------------- |
| `--theme <ID>`  | 指定主题 ID（必需）    | `--theme 68c3da6b308b8b294cf01328`                 |
| `--only <路径>` | 只推送指定文件（推荐） | `--only "sections/main-article/main-article.html"` |
| 无选项          | 推送整个主题（慎用）   | `theme push --theme <ID>`                          |

#### 推荐命令（单文件推送）

```powershell
# 推送单个文件（最安全、最快）
npx @shoplineos/cli theme push --theme 68c3da6b308b8b294cf01328 --only "sections/main-article/main-article.html"

# 推送某个目录下所有文件
npx @shoplineos/cli theme push --theme 68c3da6b308b8b294cf01328 --only "sections/main-article/*.html"

# 推送多个指定文件（不推荐，分别推送更好）
npx @shoplineos/cli theme push --theme 68c3da6b308b8b294cf01328 --only "sections/main-article/main-article.html" --only "sections/main-article/main-article.css"
```

### 3. ⚠️ 常见推送失败原因及解决方案

#### 失败原因 1：语法错误（最常见）

**症状**：

```
[error]: parse error, line:312 col:27
```

**原因**：

- 使用了不支持的 Liquid 语法
- 常见错误：`变量名.[索引]`、`for item, index`、`@index` 等

**解决方案**：

1. 查看错误信息定位到具体行号
2. 参考本文档"不支持的语法"章节
3. 修复语法错误
4. 重新推送

**示例修复**：

```powershell
# 如果是数组访问错误，使用正则批量修复
(Get-Content "sections/main-article/main-article.html" -Raw -Encoding UTF8) `
  -replace '(\w+)\.\[(\d+)\]', '$1[$2]' `
  | Set-Content "sections/main-article/main-article.html" -Encoding UTF8 -NoNewline
```

#### 失败原因 2：路径错误

**症状**：

```
Error: File not found: sections/xxx/yyy.html
```

**原因**：

- 文件路径拼写错误
- 使用了反斜杠 `\` 而非正斜杠 `/`
- 文件不存在

**解决方案**：

```powershell
# ❌ 错误：使用反斜杠
--only "sections\main-article\main-article.html"

# ✅ 正确：使用正斜杠
--only "sections/main-article/main-article.html"

# 验证文件是否存在
Test-Path "e:\Bottle1\sections\main-article\main-article.html"
```

#### 失败原因 3：编码问题

**症状**：

- 推送成功，但页面显示乱码
- 中文字符显示异常

**解决方案**：

```powershell
# 确保文件使用 UTF-8 编码保存
Get-Content "file.html" -Encoding UTF8 | Set-Content "file.html" -Encoding UTF8
```

在 VS Code 中：

1. 右下角查看编码
2. 点击编码 → "通过编码保存" → 选择 "UTF-8"

#### 失败原因 4：CLI 未登录或 Token 过期

**症状**：

```
Error: Authentication required
```

**解决方案**：

```powershell
# 重新登录
npx @shoplineos/cli theme login

# 按提示输入：
# - Store URL: fominte.myshopline.com
# - Access Token: （从 Shopline 后台获取）
```

#### 失败原因 5：网络超时

**症状**：

- 长时间卡在 "Syncing theme..."
- 无任何错误信息

**解决方案**：

```powershell
# 1. 按 Ctrl+C 终止
# 2. 检查网络连接
# 3. 重新推送
npx @shoplineos/cli theme push --theme <ID> --only "路径"
```

### 4. 📋 推送前检查清单

**每次推送前必查**：

- [ ] 文件已保存（VS Code 标签页无圆点）
- [ ] 语法无明显错误（无红线提示）
- [ ] 路径使用正斜杠 `/`
- [ ] 文件使用 UTF-8 编码
- [ ] CLI 已登录（之前推送过则无需重新登录）
- [ ] 网络连接正常

### 5. 🔍 调试技巧

#### 技巧 1：逐步验证

```powershell
# 不要一次推送整个主题，分步推送
# 第一次：最小模板
# 第二次：添加一个功能
# 第三次：再添加一个功能
# ...依此类推
```

#### 技巧 2：使用 Git 版本控制

```powershell
# 推送前先提交
git add .
git commit -m "添加 FAQ 模块"

# 推送失败后回滚
git checkout HEAD -- sections/main-article/main-article.html

# 重新修复并推送
```

#### 技巧 3：保留推送日志

```powershell
# 将推送输出保存到日志文件
npx @shoplineos/cli theme push --theme <ID> --only "file.html" > push-log.txt 2>&1

# 查看日志
Get-Content push-log.txt
```

### 6. ❓ 常见问题 FAQ

**Q1：为什么别人能推送成功，我却失败？**

A：最常见的原因是**语法错误**。成功推送的人可能：

1. 使用了正确的语法
2. 推送前先修复了错误
3. 使用了更早的无错误版本

**Q2：如何知道 Theme ID？**

A：

```powershell
# 方法 1：从之前成功推送的输出中查看
# 方法 2：从 Shopline 后台主题编辑器 URL 中提取
# URL 示例：https://fominte.myshopline.com/admin/theme/editor?themeId=68c3da6b308b8b294cf01328
```

**Q3：可以同时推送多个文件吗？**

A：技术上可以，但**不推荐**。建议分别推送，这样：

- 容易定位错误
- 失败后只需修复一个文件
- 更安全（不会一次性覆盖多个文件）

**Q4：推送后需要刷新缓存吗？**

A：是的，推送成功后务必**强制刷新**页面（Ctrl + F5）以查看最新效果。

---

## 📐 模板文件结构

### Section 文件组成

```
sections/
  main-article/
    ├── main-article.html    # 主模板（必需）
    ├── main-article.css     # 样式文件（可选）
    └── main-article.json    # Schema配置（可选）
```

### HTML 模板结构

```handlebars
{{!-- 1. 引入 CSS --}}
{{#component "stylesheet" src="./main-article.css" | asset_url() /}}

{{!-- 2. 内联样式（优先于CSS文件）--}}
<style>
  /* 自定义样式 */
</style>

{{!-- 3. HTML 主体 --}}
<div class="main-article-container">
  {{!-- 访问 Metafields --}}
  {{#if article.metafields.custom.field_name}}
    {{article.metafields.custom.field_name}}
  {{/if}}

  {{!-- 默认内容块 --}}
  {{#content "blocks" /}}
</div>

{{!-- 4. Schema（JSON-LD）--}}
<script type="application/ld+json">
{
  "@context": "http://schema.org",
  "@type": "Article",
  "headline": "{{article.title}}"
}
</script>

{{!-- 5. Schema 配置 --}}
{{#schema}}
{
  "name": "Section Name",
  "settings": [],
  "blocks": []
}
{{/schema}}
```

---

## 🎨 CSS 样式技巧

### 1. 使用内联 `<style>` vs 外部 CSS

```html
<!-- ✅ 推荐：内联样式（加载快） -->
<style>
  .pseo-article {
    color: #333;
  }
</style>

<!-- ⚠️ 外部文件（需要额外请求） -->
{{#component "stylesheet" src="./main-article.css" /}}
```

### 2. 响应式设计

```css
/* 移动优先 */
.pseo-decision {
  grid-template-columns: 1fr;
}

/* 桌面端 */
@media (min-width: 768px) {
  .pseo-decision {
    grid-template-columns: 1fr 1fr;
  }
}
```

### 3. 命名约定

- 使用前缀避免冲突（如 `pseo-`）
- 语义化命名
- BEM 方法论（可选）

```css
/* ✅ 好的命名 */
.pseo-article {
}
.pseo-article-meta {
}
.pseo-decision-box {
}

/* ❌ 通用命名（可能冲突） */
.article {
}
.meta {
}
.box {
}
```

---

## 🔍 Metafields 访问语法

### Page Metafields

```handlebars
{{page.metafields.custom.field_name}}
```

### Blog Post (Article) Metafields

```handlebars
{{article.metafields.custom.field_name}}
```

### Product Metafields

```handlebars
{{product.metafields.custom.field_name}}
```

### 条件渲染

```handlebars
{{#if article.metafields.custom.field_name}}
  字段存在时显示
{{else}}
  字段不存在时显示
{{/if}}
```

### 数据处理

```handlebars
{{!-- 字符串分割 --}}
{{#var items = article.metafields.custom.list_field | split("||") /}}

{{!-- 遍历 --}}
{{#for item in items}}
  {{item}}
{{/for}}

{{!-- 二次分割 --}}
{{#var parts = item | split("??") /}}
{{parts[0]}}  {{!-- 第一部分 --}}
{{parts[1]}}  {{!-- 第二部分 --}}
```

---

## ⚠️ 常见错误及解决方案

### 错误 1：Metafields 不显示

**症状**：页面上看不到 Metafields 数据

**可能原因**：

1. Metafields 未正确创建
2. 命名空间/键名拼写错误
3. 模板语法错误
4. 模板未推送到 Shopline

**解决步骤**：

```
1. 在 Shopline 后台检查 Metafields 是否存在
   「设置」→「自定义数据」→「博客文章」

2. 确认字段名拼写正确
   custom.pseo_hero_title ✓
   custom.pseo_herotitle  ✗

3. 在文章编辑页面填充数据
   滚动到「自定义数据」区域

4. 确认模板已推送
   npx @shoplineos/cli theme push --only "..."

5. 刷新页面并清除缓存
```

### 错误 2：CLI 推送卡住

**症状**：CLI 长时间显示 "Syncing theme..."

**解决方案**：

```powershell
# 1. 终止进程（Ctrl+C）
# 2. 重新登录
npx @shoplineos/cli theme login

# 3. 检查网络连接
# 4. 重新推送
```

### 错误 3：样式不生效

**可能原因**：

1. CSS 选择器被覆盖
2. 样式优先级不够
3. CSS 语法错误

**解决方案**：

```css
/* 使用 !important 提升优先级（谨慎使用） */
.pseo-article h2 { color: #333 !important; }

/* 增加选择器权重 */
.main-article-container .pseo-article h2 { color: #333; }

/* 使用内联样式（最高优先级） */
<h2 style="color: #333;">标题</h2>
```

---

## 📊 数据结构示例

### How-to Steps

```
字段：how_to_steps（多行文本）
格式：标题??描述||标题??描述

示例：
Identify Your Design Style??Determine whether your gown is traditional, modern, bohemian, or vintage.||Select Base Fabric??Choose between rigid (Guipure) for structure or soft (Chantilly) for drape.||Request Samples??Order A4 swatches from at least 3 suppliers.
```

### Comparison Table

```
字段：comparison_table（多行文本）
格式：列1??列2??列3||列1??列2??列3

示例（第一行是表头）：
Feature??Option A??Option B||Structure??Rigid??Soft||Price??$15-25/yard??$8-15/yard||MOQ??1000 yards??500 yards
```

### FAQ

```
字段：faq_items（多行文本）
格式：问题??答案||问题??答案

示例：
What is the MOQ???Our standard MOQ is 500 yards per design.||How long does shipping take???15-20 days from payment confirmation.
```

---

## 🎓 最佳实践总结

### 1. 开发流程

```
需求分析
  ↓
设计 Metafields 结构
  ↓
创建最小可行模板（MVP）
  ↓
推送验证
  ↓
逐步添加功能
  ↓
每次添加后推送验证
  ↓
完整测试
```

### 2. 测试清单

- [ ] 在 Shopline 后台创建所有 Metafields
- [ ] 创建测试文章并填充示例数据
- [ ] 本地验证模板语法无错误
- [ ] 使用 CLI 推送模板
- [ ] 刷新页面查看效果
- [ ] 测试所有条件分支（字段存在/不存在）
- [ ] 测试响应式布局（手机/平板/桌面）
- [ ] 验证 SEO 元素（TDK、Schema）

### 3. 维护建议

1. **版本控制**：
   - 使用 Git 管理模板代码
   - 每次重大改动打 tag
   - 保留可用版本备份

2. **文档记录**：
   - 记录每个 Metafield 的用途
   - 记录数据格式要求
   - 记录已知问题和解决方案

3. **性能优化**：
   - 最小化 CSS 和 HTML
   - 缓存静态资源
   - 懒加载图片

---

## 🚨 紧急问题处理

### 推送失败无法恢复

**症状**：模板推送失败，页面显示错误

**解决方案**：

```powershell
# 1. 立即回滚到上一个可用版本
git checkout HEAD~1 -- sections/main-article/main-article.html

# 2. 推送回滚版本
npx @shoplineos/cli theme push --only "sections/main-article/main-article.html"

# 3. 在本地修复问题
# 4. 重新推送
```

### Metafields 数据丢失

**预防措施**：

1. 在推送前备份 Metafields 数据（导出JSON）
2. 测试页面避免使用真实数据
3. 重要数据保存在文档中

---

## 📚 参考资源

- **Shopline 官方文档**：[theme.shopline.com/docs](https://theme.shopline.com/docs)
- **Liquid 语法参考**：[liquidjs.com](https://liquidjs.com/)
- **本项目经验**：
  - `pseo-solution-analysis.md` - 方案选择分析
  - `blog-metafields-definition.md` - 完整字段定义
  - `task.md` - 实施任务清单

---

## ✅ 快速检查清单

开始新功能前检查：

- [ ] Metafields 已在后台创建
- [ ] 测试数据已准备
- [ ] 了解 Shopline 语法限制
- [ ] 从最小版本开始
- [ ] 每次改动后验证

推送前检查：

- [ ] 本地代码语法无错误
- [ ] 移除调试代码
- [ ] CSS 样式已优化
- [ ] 响应式已测试
- [ ] 备份当前可用版本

---

**最后更新**：2026-02-07  
**维护者**：Fominte 技术团队
