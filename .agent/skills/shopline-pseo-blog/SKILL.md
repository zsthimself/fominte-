---
name: shopline-pseo-blog
description: |
  Shopline pSEO 博客批量生成系统。用于从关键词数据库分析搜索意图，
  自动化生成、发布并渲染 pSEO 博客文章。
  使用场景：(1) 批量生成产品×场景矩阵博客；(2) 通过 API 自动创建博客并填充元字段；
  (3) 前端模板渲染元字段内容。
---

# Shopline pSEO Blog 系统

> **核心理念**：AI 分析意图 → 生成内容 → API 创建博客 → 传入元字段 → 前端拼接渲染

---

## 🎯 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                         pSEO Blog 工作流                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────┐       │
│  │  关键词库    │ →  │  意图分析   │ →  │  内容生成 (AI)   │       │
│  │ (选题矩阵)   │    │ (搜索意图)   │    │ (fominte-b2b)   │       │
│  └─────────────┘    └─────────────┘    └────────┬────────┘       │
│                                                  ↓                │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────┐       │
│  │  前端渲染   │ ←  │  元字段映射  │ ←  │  API 发布       │       │
│  │ (Handlebars) │    │ (JSON→元字段)│    │ (Shopline REST) │       │
│  └─────────────┘    └─────────────┘    └─────────────────┘       │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📦 元字段 Schema (Metafields)

所有元字段使用 `custom` 命名空间，在 Shopline 后台定义于：  
**设置 → 自定义数据 → 博客文章 (Blog posts)**

### 基础字段

| 字段名               | 类型     | 说明     | 示例                              |
| -------------------- | -------- | -------- | --------------------------------- |
| `reading_time`       | 单行文本 | 阅读时间 | `8 min read`                      |
| `article_type`       | 单行文本 | 文章类型 | `how-to` / `guide` / `comparison` |
| `article_intro`      | 多行文本 | 文章摘要 | 一段话介绍                        |
| `article_conclusion` | 多行文本 | 结论段落 | 总结性内容                        |

### 内容模块字段

| 字段名                   | 类型     | 说明     | 数据格式                                                   |
| ------------------------ | -------- | -------- | ---------------------------------------------------------- |
| `concept_explanation`    | 多行文本 | 概念解释 | 纯文本                                                     |
| `step_by_step_tutorials` | 多行文本 | 操作步骤 | `标题??材料??时间??步骤??注意事项\|\|...`                  |
| `scenario_matrix`        | 多行文本 | 场景决策 | `方法名??适用场景1\|场景2\|场景3??避免场景1\|场景2\|\|...` |
| `common_mistakes`        | 多行文本 | 常见错误 | `错误描述??后果??解决方案\|\|...`                          |
| `faq_items`              | 多行文本 | FAQ      | `问题???答案\|\|问题???答案`                               |

### 分隔符规范

- **一级分隔符**：`||` — 分隔列表项
- **二级分隔符**：`??` — 分隔字段内部
- **三级分隔符**：`|` — 分隔子列表（如场景列表）
- **FAQ分隔符**：`???` — 分隔问答对

---

## 🔧 核心脚本

### 1. 内容生成 → 元字段格式

生成博客内容后，需要将各模块转换为元字段格式：

```javascript
// 示例：FAQ 转换
const faqs = [
  { q: "What is MOQ?", a: "Minimum Order Quantity, typically 500 yards." },
  { q: "Lead time?", a: "30-45 days for production." },
];

const faq_metafield = faqs.map((item) => `${item.q}???${item.a}`).join("||");

// Output: "What is MOQ????Minimum Order Quantity, typically 500 yards.||Lead time????30-45 days for production."
```

### 2. API 发布博客

```bash
# 创建博客文章
POST /admin/openapi/v20251201/store/blogs/{blog_collection_id}/articles.json

# 请求体
{
  "blog": {
    "title": "文章标题",
    "handle": "article-seo-slug",
    "content_html": "<p>正文 HTML</p>",
    "digest": "摘要",
    "author": "Stephen Chen",
    "published": true,
    "template_name": "templates/blogs/detail.json"
  }
}
```

### 3. 填充元字段

```bash
# 通过 API 填充元字段（需独立请求）
POST /admin/openapi/v20251201/store/blogs/{blog_collection_id}/articles/{article_id}/metafields.json

# 请求体
{
  "metafield": {
    "namespace": "custom",
    "key": "reading_time",
    "value": "8 min read",
    "type": "single_line_text_field"
  }
}
```

---

## 🖥️ 前端模板渲染

### 元字段访问方式

> ⚠️ **关键技术**：必须使用 `get_metafield()` 函数，直接访问 `article.metafields` 不工作

```handlebars
{{!-- 正确方式 --}}
{{#var reading_time = article | get_metafield("custom", "reading_time") /}}
{{reading_time.value}}

{{!-- 错误方式（不工作）--}}
{{article.metafields.custom.reading_time.value}}
```

### 模板文件位置

- **主模板**：`sections/main-article/main-article.html`
- **样式文件**：`sections/main-article/main-article.css`

### 模板结构（参考阿里巴巴 pSEO）

```
┌─────────────────────────────────┐
│ Header: 标题 + 元信息 + 封面图   │
├─────────────────────────────────┤
│ Intro: 文章摘要                  │
├─────────────────────────────────┤
│ Concept: 概念解释                │
├─────────────────────────────────┤
│ Content: 主体内容（编辑器）       │
├─────────────────────────────────┤
│ Tutorials: 步骤教程              │
├─────────────────────────────────┤
│ Scenarios: 场景决策矩阵          │
├─────────────────────────────────┤
│ Mistakes: 常见错误               │
├─────────────────────────────────┤
│ FAQ: 常见问题                    │
├─────────────────────────────────┤
│ Conclusion: 结论                 │
├─────────────────────────────────┤
│ Footer: 作者卡片 + 分享 + 评论   │
└─────────────────────────────────┘
```

---

## 📋 完整工作流

### Phase 1: 选题准备

1. 从 `blog-content-pipeline.md` 选择目标关键词
2. 确定文章类型（how-to / guide / comparison）
3. 分析搜索意图（使用 `fominte-b2b-editor` Phase 1）

### Phase 2: 内容生成

1. 调用 `fominte-b2b-editor` 生成博客内容
2. 输出结构化数据（各模块对应元字段）
3. 人工审核或 AI 自检通过

### Phase 3: 元字段格式化

1. 将各内容模块转换为元字段格式
2. 验证分隔符正确性
3. 输出 JSON 数据包

### Phase 4: API 发布

1. 创建博客文章（获取 article_id）
2. 填充所有元字段
3. 验证发布状态

### Phase 5: 前端验证

1. 访问博客页面
2. 检查所有模块显示正确
3. 移动端适配测试

---

## 📚 相关资源

| 文件                                                                       | 说明           |
| -------------------------------------------------------------------------- | -------------- |
| [blog-content-pipeline.md](../fominte-b2b-editor/blog-content-pipeline.md) | 选题库         |
| [blog-metafields-data-guide.md](../../docs/blog-metafields-data-guide.md)  | 元字段测试数据 |
| [shopline-developer-guide.md](../../docs/shopline-developer-guide.md)      | API 完整文档   |
| [shopline-development-guide.md](../../docs/shopline-development-guide.md)  | CLI 推送技巧   |
| [main-article.html](../../sections/main-article/main-article.html)         | 前端模板       |

---

## ⚠️ 重要注意事项

1. **元字段必须先在后台定义**：设置 → 自定义数据 → 博客文章
2. **分隔符避免冲突**：不要在文本中使用 `||` 或 `??`
3. **API 版本**：使用 `v20251201`（稳定版）
4. **CLI 推送**：语法 `parts.[0]` 改为 `parts[0]`
5. **权限要求**：需要 `read_blogs` 和 `write_blogs` 权限
6. **模板名称**：pSEO 文章使用 `article.pseo` 模板

---

## 📈 开发进度

| 阶段                  | 状态   | 说明                                 |
| --------------------- | ------ | ------------------------------------ |
| ✅ 元字段 Schema 设计 | 完成   | 2026-02-09                           |
| ✅ 前端模板开发       | 完成   | `main-article.html` 支持所有模块渲染 |
| ✅ pSEO 模板配置      | 完成   | `article.pseo.json` 禁用系统组件     |
| ✅ 样式优化           | 完成   | 字号 20px、换行修复、作者头像        |
| ✅ 开发文档           | 完成   | `shopline-dev-guide.md` 踩坑指南     |
| 🔄 API 脚本测试       | 进行中 | 需配置 Access Token                  |
| ⏳ 批量生成流程       | 待开始 | 依赖 API 测试通过                    |

---

## 🔧 API 测试前置条件

### 1. 获取 Access Token

在 Shopline 后台创建私有应用：

```
设置 → 应用管理 → 创建私有应用 → 获取 Access Token
```

**所需权限**：

- `read_blogs` - 读取博客
- `write_blogs` - 写入博客

### 2. 获取 Blog Collection ID

访问 Shopline REST API 获取博客列表：

```bash
GET https://fominte.myshopline.com/admin/openapi/v20251201/store/blogs.json
```

### 3. 配置 .env 文件

在 `scripts/` 目录创建 `.env` 文件：

```
SHOPLINE_STORE_DOMAIN=fominte.myshopline.com
SHOPLINE_ACCESS_TOKEN=your_access_token
SHOPLINE_BLOG_COLLECTION_ID=your_blog_collection_id
```

### 4. 运行测试

```bash
cd e:\Bottle1\.agent\skills\shopline-pseo-blog\scripts
npm install dotenv
node create-pseo-blog.js ../examples/example-blog-data.json
```

---

**创建时间**：2026-02-09  
**最后更新**：2026-02-09  
**维护者**：Fominte 技术团队
